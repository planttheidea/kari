/**
 * @function isNAN
 *
 * @description
 * test if the item is NaN
 *
 * @param {*} object the object to test
 * @returns {boolean} is the item is NaN
 */
export default function isNAN(object) {
  return object !== object;
}
