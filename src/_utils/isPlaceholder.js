// placeholder
import __ from '../__';

/**
 * @function isPlaceholder
 *
 * @description
 * determine if the object passed is the placeholder
 *
 * @param {*} object the object to test
 * @returns {boolean} is the object the placeholder
 */
export default function isPlaceholder(object) {
  return object === __;
}
