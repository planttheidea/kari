/**
 * @function isUndefined
 *
 * @description
 * test if the item is undefined
 *
 * @param {*} object the object to test
 * @returns {boolean} is the item is undefined
 */
export default function isUndefined(object) {
  return object === void 0;
}
