// methods
import findLastIndex from './findLastKey';

/**
 * @function findLastIndex
 *
 * @description
 * find the index of an item in the collection passed starting from the end of the collection
 * and iterating to the front
 *
 * @param {function} fn the function to find the matching item with
 * @param {Array<any>|Object} collection the collection to find the item from
 * @returns {number} the matching item index
 */
export default findLastIndex;
