// external dependencies
import {identity} from './_internal/normalize';

/**
 * @function identity
 *
 * @description
 * return the first argument passed
 *
 * @param {any} value the value to pass
 * @returns {<T>} the first value passed
 */
export default identity;
